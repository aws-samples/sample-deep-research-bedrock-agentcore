# Chat Agent - Minimal dependencies
# Only Strands Agent + AgentCore Memory needed

# Core Strands Agent framework (includes bedrock runtime dependencies)
bedrock-agentcore[strands-agents]>=0.1.0

# Basic utilities
pydantic>=2.0.0
python-dotenv>=1.0.0
nest-asyncio>=1.5.0

# HTTP client for API calls
httpx>=0.27.0
